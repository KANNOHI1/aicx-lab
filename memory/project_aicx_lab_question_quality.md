---
name: aicx-lab の作問品質チェック（長さ手がかり・型3順位語）
description: 作問Buildのたびに機械チェックで潰す項目と、その閾値・理由。B5の敵対的レビューで発見された
type: project
last_verified: 2026-08-29
---

作問Build（B2〜B11・C2・D1・D2）では、実装後・commit 前に **node スクリプトで機械チェック**を通す。モデルに判断させない。

**チェック項目と閾値**

| 項目 | 基準 |
|---|---|
| 正解肢が4択で最長の割合 | **7割未満**に抑える |
| 正解が2位より25%以上長い問題（「強い手がかり」） | **0〜1件**に抑える |
| type:3 の問題文に順位語（「最も優先」「最初に」）があるか | 全件必須（DESIGN §3.2） |
| ID 重複・ケース字数120〜250・limit:180・4択 | 違反0 |
| 新規用語が教材本文に完全一致で出現するか | 全件（linkify は本文のみ対象、`kw` チップは対象外） |

**Why**: 選択肢はシャッフルされるので「いつもAが正解」の手がかりは消えるが、**文字数はシャッフルしても変わらない**。正解肢は条件を全部書くので自然に長くなり、誤答は一言で済むため短くなる。結果「一番長いのを選ぶ」で正答できてしまい、**模試プールが習熟度の計器として機能しなくなる**。HK が「仕上がった」と誤判定するのが最大の害。

**発見の経緯**: 2026-08-28、B5 で初めて敵対的レビュー workflow（Opus×28体）を回したところ、この欠陥を含む4件が検出された。それ以前の B1〜B4 は未検証で、**第2回に13件・第3回6件・第1回4件・第4回4件が未修正のまま本番に残っている**（要一括是正）。B6・B7 は実装中に自己適用して1件以下に抑えた。

**How to apply**: 実装スクリプト実行後、node のワンライナーで QB を eval して集計する（`index.html` から `const QB=[...]` を正規表現で抜いて eval）。閾値超過分は**誤答肢を長くする**方向で直す（正解を削ると情報が落ちるため）。

関連: [[project_aicx_lab_build_ops]]
