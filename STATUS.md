# STATUS.md

> **Build の進行状態の正は [BUILD_PLAN.md](BUILD_PLAN.md) の進行表**。本ファイルはセッション横断の現在地・判断・ハマりパターンだけを持つ（進行表は複製しない）。仕様の正は [DESIGN.md](DESIGN.md)。

## 現在地

- **Build F6a・F6b 完了。第6章が終わり、これで全11回＝全32セクションが公式テキスト準拠になった**。全面再構築（F シリーズ）は F0〜F6b が完了し、残りは **F7（用語集の再構築）→ F8（総復習ページ・1枚図・型名統一）→ F9（模試再組成）→ F10（最終検証）**
- F6a で是正: 旧**「選定基準4軸（頻度×工数×リスク×データ状態）」「Definitionの4点（目的・スコープ・成功条件・制約）」「Designの5点（業務フロー/データ/プロンプト/ツール連携/人の介入点）」「介入設計」はいずれも公式に存在しない**ため全廃。公式は Discovery＝**ボトルネック分析→AI適合性診断→優先順位マトリクス**の3手法と**2段階のフィルタリング**（「ボトルネックだからAI化」ではなく「ボトルネックであり、かつAIに向いている業務を選ぶ」）／**AI適合性3基準**（非構造化データ／文脈判断／ミス許容かHITLで補完）と**適合性が低い4類型**／**優先順位マトリクスは「効果の大きさ」×「実現のしやすさ」の2軸**（効果大×実現易＝クイックウィン、効果大×実現難＝中長期の検討対象）。Definition＝**業務フローチャート・要件定義書・ペルソナ定義・合格基準**の4成果物、要件定義書は**機能要件（何をするか）・非機能要件（どの水準で）・制約条件・前提条件**の4要素、**ペルソナは属性でなく「出力をどう使って何を判断するか」＋間接ユーザー**、**合格基準＝PoCの合否を判定する一回限りの基準（KPIとは別）**。Design＝**コンテキスト設計・I/O設計・セキュリティ設計**の3領域で、**「この設計論点はどの領域か」を公式が直接出題**する
- F6b で是正: 旧**「Development の検証3観点＝精度・定着・効果」（これは Sec18 の3層で Sec31 の論点ではない）／「改善サイクル」（公式の枠組み名はフィードバックループ）／「Deployment & Scale の5点＝本運用移行・教育・運用ルール・横展開・継続改善」（公式0件）**を全廃。公式は Development＝**プロトタイピング・サンドボックス・HITL・フィードバックループ**の4概念と**Go/No-Go/Pivot**、Deployment＝**パイロット→ロールアウト→スケーリング**・**AI-SECIモデル**・**KPIモニタリング**
- 学習開始 2026-09-01 の制約は解消。**9/1 時点で全11回が公式準拠**のため範囲を区切る必要がなくなった（用語集・総復習・模試は F7〜F10 で仕上げる）
- 問題は230問中209問を差し替え済み。残る21問（Sec18・Sec27 の20問は Build E 済みで維持）
- 指摘の一次資料は [docs/REVIEW_FINDINGS.md](docs/REVIEW_FINDINGS.md)、再構築の進行表は BUILD_PLAN.md の F シリーズ、各回の設計は DESIGN.md §4、公式側の記述ゆれは DESIGN.md §7
- 最新リモート同期: 2026-08-30 19:07 JST — origin/main = HEAD (ahead 0 / behind 0)

## 次にやること

1. **Build F7**（次セッション）: 用語集の再構築。第6章の語は F6a/F6b で追加済み（137語）なので、残りは**参照 Section 番号の点検・重複エントリの統合・対で問われる語を対で立てる**が主。qcheck の「用語集にあるが公式テキストに同表記で出現しない語」（現時点2語＝組織文化の診断3観点・SECIモデル系の残り）もここで裁定する [Claude]
2. Build F8（総復習ページ第12〜14回の再作成＋総まとめ1枚図＋TYPENAME と第13回の型名統一）→ F9（模試①②③の再組成。QB の ID/sec/ch/type/pool は F シリーズで不変に保ったため `tools/mockplan.js` で再現できるはず）→ F10（最終検証） [Claude]
3. 申込締切 9/20 までに受験申込を済ませる（HK 作業） [Claude]

## 未解決・保留

| 項目 | 状態 | メモ |
|---|---|---|
| 出題数・配点・合格基準 | 非公式のまま | 50問/100点・8割は代理指標。公式ガイド or 受験実体験で確定（DESIGN §7） [Claude] |

## 判断済みの決定

> 2026-08-30 以前の判断済みの決定 16 件は [STATUS_ARCHIVE.md](STATUS_ARCHIVE.md) へ移動。

| 決定 | 理由 | 日付 |
|---|---|---|
| 公式テキストの書き起こし一式は `text_ocr/` に置き git 管理外とする（.gitignore 済み） | HK の私的照合用。リポジトリにもサイトにも本文を出さない | 2026-08-29 [Claude] |
| 書き起こしの検証は「A/B の二重キー入力 + 機構の異なる第3のOCR」の三重で完了とする。第3検証は全文一致率ではなく**数値と英字用語だけ**を照合する | 同系統モデル同士のA/Bは「揃って同じ誤読をする」穴が残る。機構の違うTesseractを通すとその穴を塞げる。ただしTesseractは写真の漢字認識が弱く全文比較は無意味なので、誤読しても壊れにくく間違うと実害が大きい要素（数値・英字略語）だけを見る。実際この設計で要確認35件まで絞れ、全件裁定してマスター欠陥ゼロを確認できた | 2026-08-29 [Claude] |
| 印字ページ番号アンカーはこれ以上増やせない。順序の裏付けは**本文側の連番（CHAPTER・Section・5D段階見出し）**で代替する | 前半の写真はPDFビューアをスクロールした画面の撮影で、紙面下端のノンブルが画角に入っていない。ノンブル専用OCRを組んで較正したが既知46点に対し19点しか当たらず、原因が「切り出し位置の調整不足」ではなく「そもそも写っていない」ことが判明した。見出しの連番なら本文に写っており、昇順・欠番なしを確認できる | 2026-08-29 [Claude] |
| 図の細部は収録しない方針を維持し、代わりに `text_ocr/FIGURES.md` で「どこに図があるか」を索引化する | 図を全部起こし直すと、それ自体が誤りうる第2の正データを作ってしまう。恒久ルール「書き換える時は png を開く」を実行可能にするには、所在が分かれば足りる。図主体24ページを優先確認として分離した | 2026-08-29 [Claude] |
| 公式テキストPDFの購入者透かし（別名義）は**会社で購入し正式にシェアされたもの**。入手経路の問題なし・本件クローズ | HK 確認（2026-08-29）。禁止事項の「流出版の探索」に抵触しないことを確認済み。以後この透かしを理由に入手経路を再確認しない | 2026-08-29 [Claude] |
| 透かしの氏名・メールアドレスは書き起こし成果物に残さない（除去済み） | 他者の個人情報であり、書き起こしに保持する必要がない。`AICX_official_text.md` 3行・`md/` 3行・`adjudicate/` 2行を削除し全ファイルで残存ゼロを確認。png は焼き込みのため除去不可だが git 管理外で外に出ない。`tools/verify3.py` の STOP でも除外済み | 2026-08-29 [Claude] |
| **模試3セットの問題割付は3セット分（125問）を一括で解いてから切り出す。①だけ先に組まない** | 章別と型別の2つの配分を同時に満たす必要があり、①が mock プールの希少セル（特に型1）を先取りすると②③が組めなくなる。実測で ②が lesson 再利用16問となり上限15を超えた。mock を各セットへ比例配分し、取り分超過を高コストで許容する形にして ①15/②10/③6 に収めた。手順は `tools/mockplan.js` に保存済みで、QB 不変なら同じ ID 列を再現できる | 2026-08-29 [Claude] |
| 模試③（短縮25問）のブループリントは50問版の半分。端数は型4へ寄せる | 章 4/3/5/4/4/5、型 6/4/4/4/3/2/2。DESIGN §2 に明記した | 2026-08-30 [Claude] |
| 第12〜14回は問題を持たないガイドページとする（作問クォータの対象外） | この3回は新しい知識を足す回ではなく、模試の受け方・弱点の潰し方・直前確認を扱う運用ガイド。想起クイズだけは既存の仕組みで出る | 2026-08-30 [Claude] |
| 教材はシラバスでなく**公式テキストの「学習のポイント」**を骨格にする | シラバスのキーワードから推測して構成した結果、公式が本文で定義する枠組みを47件取りこぼした。公式は各Sectionの冒頭で「試験では〜が問われる」と論点を名指ししており、そこが唯一の正 | 2026-08-30 [Claude] |
| 全面再構築を選び、学習開始を後ろ倒しにする | 間違った枠組みを覚えて落ちるリスクが、学習開始が数日遅れるコストを上回る。覚え直しのコストも発生しない | 2026-08-30 [Claude] |
| **F シリーズでは問題の ID・sec・ch・type・pool を維持し、中身（設問・選択肢・解説）だけ差し替える** | ID を変えると公開済み問題 ID の変更にあたり、sec/ch/type/pool を変えると `MOCKSETS` の125問の割付が壊れて F9 まで模試が動かなくなる。F1 でこの制約下でも公式準拠の作問は成立すると確認した | 2026-08-30 [Claude] |
| 廃止語の用語集エントリは、その語を教材から消す **F の中で** 削除・統合する（用語集の全体再構築は F7 のまま） | qcheck 項目9「用語集にあるが教材本文に無い語」は本文側から語が消えた時点で落ちる。F7 まで放置すると各 F の完了条件③（qcheck 全項目クリア）を満たせない | 2026-08-30 [Claude] |
| **サイト内の SYL（シラバス32セクション）はシラバス Ver1.2 の逐語記録として扱い、公式テキスト本文と食い違っても書き換えない** | シラバスと本文の食い違いは公式側の記述ゆれであり、こちらが片方に寄せて書き換えると出典が失われる。教材・解説の側で「シラバスはこう言うが本文の定義はこれ」と書き分け、DESIGN §7 に記録する。F2 で Sec9（シラバス＝目的・入力・処理・出力・関係者の5観点／本文＝IPO3要素）に適用した | 2026-08-30 [Claude] |
| **1つの F に詰め込む量は「そのセッションで通読する公式テキストの行数」で決める。1,000行を超えるなら分割する** | F2（約750行＋教材1回＋22問）でコンテキスト23%（Messages 162k / 1M・固定オーバーヘッド63.5k）を実測。F3 を分割せずに回すと60〜70%に達して後半で auto-compact が走り、圧縮で公式テキストの記憶が欠けたまま作問することになる＝F0 で潰したはずの「推測で書く」失敗の再発。これを根拠に F3 を3分割し、F5・F6 も2分割を見込む | 2026-08-30 [Claude] |
| **Fable は F シリーズでも投入しない。監督は Opus のまま** | 最終レビューの設計にあった③Fable×max（見落とし探し）は、②Opus 8体が深刻度「高」47件を検出して方針転換に至ったため投入前にクローズした。F1〜F10 は DESIGN §4 が公式行番号つきの指示書になっている実装作業で、設計判断が割れる場面が出たときに都度 HK へリマインドすれば足りる | 2026-08-30 [Claude] |
| **廃止した枠組みは、それを教える回だけでなく他の回・他の問題からの参照ごと同じ Build で掃除する。`grep -c` で0件を確認してから完了と言う** | 教材は回をまたいで相互参照しており、第4回で「5条件」を消しても第5・7・10・11回の本文と Sec14/15/22/30 の問題に「第4回の5条件」が残る。参照だけ残ると、学習者は存在しないフレームを探すことになる。qcheck の項目9は用語集しか見ないのでこれを検出できない | 2026-08-30 [Claude] |
| **学習開始は 2026-09-01。ただし 9/1 時点で公式準拠なのは第1〜6回（第1〜3章）までなので、そこに限って学習する** | HK 決定（2026-08-30）。F6 完了を待つ案もあったが、HK が 9/1 開始を選択。第7回以降を先に読むと未再構築の推測フレームを覚えることになるため、範囲を区切る運用で両立させる | 2026-08-30 [Claude] |
| **公式が「正常」と言う挙動を、こちらが失敗パターンとして教えていないか点検する** | F3c で旧版が「PoC貧乏＝PoCを繰り返すのは失敗」と教えていたのに対し、公式は「繰り返すこと自体は失敗ではなくAIプロジェクトの正常な進行」と明言していた。単に語が違うのではなく**評価の向きが逆**で、そのまま覚えると本番で真逆の判断をする。廃止語の掃除では「語が公式に無い」だけでなく「公式の評価と逆になっていないか」まで見る | 2026-08-30 [Claude] |
| **推進体制の類型名は「ハイブリッド型」を正とし、「CoE型」はシラバス側の表記として SYL にのみ残す** | 公式テキスト本文の3類型は中央集権型・分散型・ハイブリッド型で、CoE は類型名ではなく推進の専門組織（L6440）。シラバス Ver1.2 は「CoE型」と書くが SYL は逐語記録なので書き換えない。教材・用語集はハイブリッド型で統一し、**s24-01 の誤答肢としてのみ「CoE型」の語を残して取り違えを潰す** | 2026-08-30 [Claude] |
| **廃止語の掃除は「その語が公式に無いか」だけでなく「他回からの参照が壊れないか」まで見る** | F5b で「チェンジマネジメント4段階」を廃止した結果、第11回・Sec32 の3箇所に残った「第9回の定着支援」という参照が指す先を失った。語を消した回だけを見ていると、参照側が存在しないフレームを指したまま残る | 2026-08-30 [Claude] |
| **1セッションで2 Build 回してよい（コンテキストに余裕がある場合）。判定は /context の実測値で行う** | F5a 完了時点で 24%（240k/1M）だったため HK 判断で F5b を継続し、両方を完了できた。通読量ベースの事前分割（1,000行超なら分割）は Build 単位の見積りであって、1セッションに1 Build という上限ではない | 2026-08-30 [Claude] |
| **第6章の Section 番号のずれは公式側の誤りとみなし、教材では正しい回・Section を示す** | 第6章本文は「Section25で学んだクイックウィン」「Section23で学んだアウトプット指標とアウトカム指標」と自章内で参照するが、実際の所在はクイックウィン＝Sec26・アウトプット/アウトカム指標＝Sec25。SYL（シラバス逐語）とは違い、これは本文どうしの相互参照なので片方が単純に誤っている。DESIGN §7 に記録した | 2026-08-30 [Claude] |
| **総まとめ1枚図は F6b で公式の枠組みへ更新するが、総復習ページの再作成と型名統一は F8 のまま** | 1枚図に非公式の枠組み（4軸・Designの5点・Deploymentの5点）が残ると、直前に誤った枠組みを固定してしまう。表の中身だけ先に正し、ページ構成の作り直しは予定どおり F8 で行う | 2026-08-30 [Claude] |
| **用語集の重複キーは JS の後勝ちで前の定義が黙って捨てられるため、qcheck では検出できない欠陥として別途点検する** | `GLO` はオブジェクトリテラルなので同名キーが2回あっても構文エラーにならず、表示は後の定義が勝つ。見た目が正しいまま古い定義が死ぬので「片方を直しても反映されない」罠になる。2026-08-30 に責任分界点・KPI の2件を統合して解消（commit 21485b9） | 2026-08-30 [Claude] |

## ハマりパターン

- **シラバスのキーワードから教材を構成すると、公式が本文で定義する枠組みを取りこぼす。** 2026-08-30 の横断レビューで深刻度「高」47件を検出（第6章はほぼ別物）。キーワードは論点の見出しにすぎず、中身の分類・段階数・名称は本文にしか書かれていない。**一次資料が手に入るまで作り込まない**か、手に入った時点で骨格から作り直す前提で進める [Claude]
- **「レビューは全Build完了後に1回だけ」の設計が遅すぎた。** 公式テキストは 2026-08-29 に書き起こしが完了していたのに、突き合わせを Build E の2論点に限定したため、全面的な乖離の発見が最終レビューまで遅れた。**一次資料が入手できた時点で、まず全体の突き合わせを1回走らせる**べきだった [Claude]

- **Bash ツールの heredoc は約 7KB で無言に切れる。** `cat > file <<'EOF'` で 300行超の JS を書こうとしたら 173行目の文字列中で truncate され、`warning: here-document ... delimited by end-of-file` だけが出た。**大きなファイル（数KB超）は Write ツールで書く**。Bash heredoc は短いスクリプト専用にする [Claude]
- **`python -m http.server` の停止確認は `netstat | grep :8777` だけでは不十分。** TIME_WAIT の行が残るため「まだ生きている」と誤読する。`grep -i listening` まで絞って初めて待受ゼロを確認できる（`pkill` は Windows で効かない。`taskkill //PID <pid> //F`） [Claude]
- **1サンプルの数値で「実害なし」と判断しない。** セッションログに `/recall` `/save` の定義文が混入する件を「13%だから無視してよい」と報告したが、HK の指摘で全ログを測り直したところ**前回セッションは40%**だった。ノイズは固定長なので**セッションが短いほど率が上がる**——そして短いセッションのログこそ読み返す対象になる。母数が1つのときは「他のケースはどうか」を先に測る [Claude]
- **測定に使う正規表現が正当な内容まで巻き込んでいないか確認する。** 上記の件で「ノイズは常に3,157字で固定」と報告したが、これは終端指定の甘い正規表現が会話本文まで拾った誤り（実際は前回2,676字・今回971字）。結論は変わらなかったが、**数字を根拠に出す前に、その数字がどう作られたかを一度点検する** [Claude]
- **index.html への大規模編集は Python スクリプト + `assert s.count(old)==1` で行う。** 置換対象が見つからないまま黙って進むのを防げる。実際 GLO の挿入位置指定を1回外したが、assert が先に落ちてファイルは無傷だった（書き込みは全 assert 通過後に1回だけ実行する構成にしておくこと） [Claude]
- **選択肢の一括置換は「選択肢1本ずつ」で行う。複数肢をまとめた文字列は改行が挟まってマッチしない。** `o:['A','B',
     'C','D']` のように整形されているため、`'B','C'` のような連結文字列は 0 件ヒットになる。assert が止めてくれたのでファイルは無傷だった [Claude]
- **短い文字列の置換は全体で一意とは限らない。** `'効果・定着・精度'` が第2回と第11回の2箇所にあり assert が検出した。前後の空白・インデントまで含めて一意にするか、周辺文字列を足して特定する [Claude]
- **選択肢を書き換えたら、解説がその選択肢を引用していないか必ず確認する。** 解説は「〜が誤りの理由：」で選択肢を要約引用しているため、文言を変えると対応が切れる。**編集前後で同じ検査を回して差分だけ見る**のが有効（元から要約引用のものが多数あり、全件検査だと141件出てノイズに埋もれる。差分なら回帰2件だけが残った） [Claude]
- **`node --check <(...)` はプロセス置換が使えず落ちる。** 一度ファイルに書き出してから `node --check file.js` する。`sed -n '/<script>/,/<\/script>/p' index.html | sed '1d;$d'` で JS 部分だけ取り出せる [Claude]
- **`process.argv[1]` はスクリプト自身のパス。引数は `process.argv[2]` から。** 間違えると自分自身を解析して意味不明なエラーになり、しかも比較対象の両方が同時に失敗するので「差分なし」と誤読しかける [Claude]
- **固定ヘッダーを持つ画面を新設したら、既存のサイトヘッダーと重ならないか目視する。** 模試の固定タイマーバーの下から既存のカウントダウンチップが覗いていた。DOM 検査では気づけず、スクリーンショットで発見した [Claude]
- **重複・同一性の判定に「先頭N文字の一致」や「包含」を使わない。必ず正規化後の類似度（SequenceMatcher ratio 等）で測る。** markdown 記号（`###` vs `##`、`**`）を除去し忘れたまま先頭200字比較していたため、ratio 0.985 の完全重複ページ（p162=p163）を取りこぼした。判定式そのものを疑う視点が要る [Claude]
- **サブエージェントに画像を読ませる時は「1枚ずつ Read し、読んだ直後に記録する」と明示する。** まとめて読んでから書かせると、ページと内容の割り当てがずれる（p298-p312 バッチで実際に発生。画像p310の内容をp308として記録していた）。検出できたのは偶然画像を直接見たからで、機械検査の網には掛かっていなかった [Claude]
- **1セッションに詰め込む Build の大きさは「通読する公式テキストの行数」で決める。** F2（Sec9-11・約750行の通読＋教材1回＋22問）でコンテキスト 23%（Messages 162k / 1M）。固定オーバーヘッドが 63.5k あるため、**通読1,000行を超える Build は分割する**。分割せずに走らせると後半で auto-compact が走り、圧縮で公式テキストの記憶が欠けたまま作問することになる＝F0 で潰したはずの「推測で書く」失敗の再発。F3 を F3a/b/c に割ったのはこの理由（2026-08-30） [Claude]
- **バックグラウンドのシェルスクリプトは即死することがある。** `bash script.sh &` 相当で走らせた tesseract ループが出力ゼロのまま終了扱いになった。Python + subprocess で書き直したら動いた。長時間ジョブは進捗を print させて確認する [Claude]
- **サブエージェントの同時実行は20体が上限**（`CLAUDE_CODE_MAX_CONCURRENT_SUBAGENTS`）。34バッチを一度に投げると後半が弾かれるので波に分ける [Claude]
- **画像OCRを重い前処理（大きなクロップ＋3倍拡大）で全ページ回すとタイムアウトする。** 領域を絞る・拡大率を下げる・対象ページを限定する。特に他のエージェントが走っている間はCPUを取り合う [Claude]

- **検証スクリプトで「期待される答え」を優先して選ぶ選択ロジックを書かない。** ノンブル抽出で候補から `配列順位-1` に一致する値を優先して返す実装にしていた。これは検証したい仮説（順位−印字=+1）を先に答えとして仕込む循環論法で、走らせれば必ず「一貫」と出る。書いた直後に気づいて最頻値方式に直した。**判定式が仮説を参照していないかを、実行前に一度見る** [Claude]
- **確認用に手早く書いた正規表現の結果を、そのまま結論にしない。** 第3検証の裁定で「p232 はマスターに数値が1つも無い」「p396 に87%・92%が無い」と読み、脱落を疑ったが、どちらも私の確認用スクリプトの問題だった（前者は半角%しか見ていなかった／後者は表示を上位14件で打ち切っていた）。マスターは両方とも正しい。**異常を見つけたら、まず自分の観測手段を疑う** [Claude]
- **画像の切り出し位置は当て推量で回さず、1枚可視化して決める。** ノンブルOCRで切り出し帯を3回変えて較正したが改善せず、実際に下端を1枚見たら「そもそもノンブルが画角外」だった。可視化1回で終わった話に、自動化の試行を3周かけた [Claude]

- **Bash の heredoc は区切り子をクォートしないと backtick を command substitution として食う。** `python - <<PY` で JS のテンプレートリテラルを含む置換スクリプトを渡したら、bash がバッククォート内を実行しようとして Python 自体が起動せず、しかも後続の `node --check` が**古いファイル**を検査して「syntax OK」と出たため成功に見えた。**`<<'PY'` とクォートする**。加えて、変更を伴うスクリプトは「変更が入ったこと」を出力で確認してから次の検証に進む [Claude]
- **GitHub Pages はブラウザキャッシュで旧版が出る。** push 後に本番を Playwright で開いたら旧コードの行番号でエラーが出た。`curl` で配信ファイルを取ると修正は入っており、単にブラウザが古い方を掴んでいただけ。**配信内容は curl で、描画はクエリ付き URL（`?v=xxx`）で確認する** [Claude]
- **新しい種類のページを足したら、既存のループ・集計が「0件」で正しく動くかを見る。** 問題を持たない第12回を足したところ、`checkDone` が「全問回答済み」と判定して 0/0 点の完了記録を作っていた（`ids.some(...)` は空配列で false）。空配列は素通りするので、エラーにならず静かに壊れる [Claude]

- **`node -e "..."` にバッククォートを含む文字列を渡さない。** JS のテンプレートリテラルや `` `node tools/qcheck.js` `` のようなコード引用がダブルクォート内にあると、bash が command substitution として実行してしまい SyntaxError になる。**変更を伴うスクリプトは Write でファイルに書いてから `node file.js` で実行する** [Claude]
- **Bash ツールの出力は 30KB 前後でファイルへ退避され、本文にはプレビュー2KBしか返らない。** 公式テキストを 400行単位で読もうとして全文が見えなかった。**1回 200行程度に割って読む**（日本語テキストで約20KB） [Claude]

- **行番号ベースで巨大ファイルを splice するときは、置換対象の開始と終了の両方を assert する。** F5a で L8_BODY を差し替えた際、終端の行番号を1つずらしたまま splice し、s24-02 の `o:[` 行と正解肢を上書きして JS の SyntaxError を出した。qcheck が eval で読むので即座に気づけたが、**assert を入口だけにすると壊れ方が静かになる**。開始・終端・その次の行の3点を確認してから splice する [Claude]
- **JS の長い文字列を node -e の中で完全一致 replace するのは不安定。** 全角記号（／・（）等）を含む定義文を `s.replace(old,new)` で置換しようとして NOT FOUND が続いた。**行番号を findIndex で特定して配列要素ごと差し替える方が確実**。文字列一致は短いキー（`'用語名':{y:`）だけに使う [Claude]

## 更新履歴

> 2026-08-30 以前の更新履歴 30 件は [STATUS_ARCHIVE.md](STATUS_ARCHIVE.md) へ移動。

- 2026-08-30 [Claude] F3 を F3a（Sec12-13,16）/ F3b（Sec14-15）/ F3c（Sec17-18）に3分割し、BUILD_PLAN 再見積りへ「通読1,000行超なら分割」の判定鍵と F2 実測（226k / 1M = 23%）を記録。commit 0442a3b
- 2026-08-30 [Claude] Build F3a（第3章前半 Sec12-13,16：教材第4回＋問題22問を公式テキスト準拠へ全面差し替え、用語集は知識流通を削除しSECIモデル・ファインチューニング・AI Readyデータ・ドリフトを追加）を実装し本番反映。旧「AIが読みやすいデータの5条件」「知識の3点検」を全廃し、他4レッスン・4問に残っていた参照も公式の語へ書き換え。DESIGN §7 に公式側の記述ゆれ3件（AI Readyデータ4特性・環境設計の呼称・7つの習慣の名称）を追記。qcheck 全20項目クリア（単独最長44%）・Playwright 実機確認・コンソールエラー0件。commit 6f6c5d6 / 9e82482
- 2026-08-30 [Claude] Build F3b（第3章中盤 Sec14-15：教材第5回＋問題22問を公式テキスト準拠へ全面差し替え、用語集はシャドーAIを削除しセマンティック検索・グラウンディング・AIポリシー・利用ガイドライン・責任分界点・エージェント台帳を追加）を実装し本番反映。旧「導入前チェック4領域」「シャドーAI」を全廃し、他レッスン・他問題の参照も掃除。F1 で第2回本文から落ちていた「ガードレール」を Sec7 の対策欄へ復帰。qcheck 全20項目クリア（単独最長44%）・Playwright 実機確認・コンソールエラー0件。commit 8a19c59
- 2026-08-30 [Claude] /save: STATUS.md・memory を更新し、セッションログを保存（本セッションで F3a・F3b の2 Build を完了）
- 2026-08-30 [Claude] Build F3c（第3章後半 Sec17：教材第6回＋問題9問を公式テキスト準拠へ全面差し替え、用語集は PoC貧乏・関門を削除し判断ゲート・実験フェーズ・本運用フェーズ・業務分析・現場視察を追加＝92語）を実装し本番反映。旧「PoC→検証→本運用の3段階」「PoC貧乏」「関門」「役割分担3者」を全廃し、第10・11・12回と他Sectionの問題に残った参照も掃除（grep 0件）。Sec18 と13問は Build E 済みのため維持。DESIGN §7 に公式側の記述ゆれ2件（Aフェーズのステップ数・現場視察/現場観察の呼称）を追記。qcheck 全20項目クリア（単独最長44%）・Playwright 実機確認（10/10・SRS B2更新）・コンソールエラー0件。**これで第1〜6回が公式準拠**。commit bfbdaac / c2e1595
- 2026-08-30 [Claude] /save: STATUS.md の同期状態行を更新し、セッションログを保存（前回 /save 以降は Build 作業なし。第1〜6回の公式準拠完了時点でクローズ）
- 2026-08-30 [Claude] Build F4（第4章 Sec19-22：教材第7回＋問題22問を公式テキスト準拠へ全面差し替え、用語集は廃止3語を削除し例外処理→エラーハンドリングへ改称・5語改訂・16語追加＝104語）を実装し本番反映。旧「自動化5段階（手作業/ツール支援/カスタムAI/AIワークフロー/AIエージェント）」「コンテキストの3点検」「連鎖の誤定義」を全廃し、他回・他Sectionの参照も掃除（grep 0件）。DESIGN §7 に公式側の記述ゆれ5件（Sec19 境界線の図／Sec22 7種類の図・ハーネス4領域の図・分割/隔離の呼称／Sec20 フォーム送信の所属分類）を追記し、いずれも本文を正とした。qcheck 全20項目クリア（単独最長41%）・Playwright 実機確認（新語16語すべてリンク化・旧語0件・採点動作・コンソールエラー0件）。**これで第1〜7回が公式準拠**。commit 17fa62b / e0650db
- 2026-08-30 [Claude] Build F5a（第5章前半 Sec23-24：教材第8回＋問題22問を公式テキスト準拠へ全面差し替え）を実装し本番反映。**旧「CoE型という類型」「権限委譲の中段＝実行して事後報告（統制の向きが逆）」「公式に無い5課題（形骸化/属人化/野良化/表層的自動化/組織抵抗）」を全廃**し、他回の参照も掃除（grep 0件・SYLは逐語のため維持）。デリゲーション4ステップ・権限委譲3段階と文脈依存性・HITLの二重の役割・組織文化の診断3観点・3類型と逆転関係・CoEの3機能・ハイブリッド型の3段階進化を新設。用語集は3語改訂・1語改称（権限委譲→デリゲーション）・1語差替・3語追加＝108語。DESIGN §7 に記述ゆれ2件を追記。qcheck 全項目クリア（単独最長45%）・実機確認（10/10・新語8語リンク化・コンソールエラー0件）。commit 2f56cd2 / 5647d61
- 2026-08-30 [Claude] Build F5b（第5章後半 Sec25-26：教材第9回＋問題15問を公式テキスト準拠へ全面差し替え）を実装し本番反映。**Sec25 は旧版が丸ごと別物のため全面新規**（アウトプット/アウトカム指標・DAU/MAUは補助指標・KGI/KPI/OKRとフェーズ別使い分け・KPI設計4ステップ・ロングテール課題・JD再設計・リスキリング判別）。Sec26 は**旧「チェンジマネジメント4段階（説明→巻き込み→教育→定着支援）」を全廃**し否認→抵抗→受容・抵抗曲線の谷・心理的安全性・Fail-Fast・クイックウィンへ差し替え。第11回/Sec32 に残った「第9回の定着支援」参照も掃除。Sec27（教材節と7問）は Build E 済みのため維持を実機で確認。用語集は3語差替・1語改訂・10語追加＝118語。DESIGN §7 に記述ゆれ2件を追記。qcheck 全項目クリア（単独最長47%）・実機確認（10/10・新語13語リンク化・コンソールエラー0件）。**これで第1〜9回が公式準拠**。commit d1a26d1 / fec5eb2
- 2026-08-30 [Claude] Build F6a（第6章前半 Sec28-30：教材第10回＋問題22問を公式テキスト準拠へ全面差し替え）を実装し本番反映。**旧「選定基準4軸」「Definitionの4点（目的・スコープ・成功条件・制約）」「Designの5点」「介入設計」を全廃**し、他回・他問題の参照も掃除（grep 0件・SYLは逐語のため維持）。Discoveryの3手法と2段階フィルタリング・AI適合性3基準と適合性が低い4類型・優先順位マトリクスの2軸・Definitionの4成果物・要件定義書の4要素・ペルソナ定義と間接ユーザー・合格基準とKPIの区別・Designの3領域と判別表・Few-shotの境界事例・Chain-of-Thoughtの使いどころ・アクセス制御とガードレールの二重を新設。用語集は3語削除・3語改訂・19語追加＝134語。qcheck 全項目クリア（単独最長47%）・実機確認（10/10・新語21語リンク化・SRS10件登録・コンソールエラー0件）。commit 8291077 / ecbc330
- 2026-08-30 [Claude] Build F6b（第6章後半 Sec31-32：教材第11回＋問題22問を公式テキスト準拠へ全面差し替え）を実装し本番反映。**旧「Developmentの検証3観点＝精度・定着・効果」「改善サイクル」「Deployment & Scale の5点」を全廃**。Developmentの4概念・プロトタイプ↔PoC↔MVP・サンドボックス↔パイロット・フィードバックループの終了判断は合格基準・Go/No-Go/Pivot・パイロットの3つの意義と確認観点・ロールアウト↔スケーリング・AI-SECIモデル・KPIモニタリングを新設。総まとめ1枚図を公式の枠組みへ更新（ページ再作成は F8）。5D の日本語表記を公式へ統一。用語集は4語削除・1語改訂・9語追加＝137語。DESIGN §7 に記述ゆれ3件を追記。qcheck 全項目クリア（単独最長50%）・実機確認（10/10・新語16語リンク化・SRS10件登録・コンソールエラー0件）。**これで全11回＝全32Section が公式準拠**。commit dd6d4c9 / 8ffb15c
- 2026-08-30 [Claude] /save: STATUS.md をローテーション（判断済みの決定13件・更新履歴27件を STATUS_ARCHIVE.md へ退避）し、現在地・次にやること・判断済みの決定・更新履歴を更新。セッションログを保存
